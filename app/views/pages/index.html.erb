<h1>SHOW BLOGS</h1>

<%= will_paginate @pages %>

<% @pages.each do |page| %>

	<div id="h1_red">
		<h1><%= page.name %></h1>
		<%= page.created_at %> | <%= link_to 'Edit', edit_page_path(page) %>
	</div>

	<span><%=raw page.content %></span>

<% end %>

<br />

<%= will_paginate @pages %>

<%= link_to 'New Page', new_page_path %>
